#!/usr/bin/env bash
# Mirror the local verification gate on every push so we catch
# the same failures locally instead of finding out from a red GHA badge.
set -e

echo "[pre-push] pnpm lint"
pnpm lint

echo "[pre-push] pnpm tsc --noEmit"
pnpm tsc --noEmit

echo "[pre-push] ok"
