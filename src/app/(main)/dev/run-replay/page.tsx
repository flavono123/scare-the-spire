import { RunReplayPoc } from "@/components/dev/run-replay-poc";

export const metadata = {
  title: "런 리플레이 — DEV",
  description: "개발 전용: STS2 .run 기반 seeded map replay PoC",
};

export default function RunReplayPage() {
  return <RunReplayPoc />;
}
