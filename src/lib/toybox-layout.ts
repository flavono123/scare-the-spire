/** Toy Box content shells. Two widths only. */

/** Tailwind `max-w-6xl` = 72rem. History Course / Transfigure / This or That / 조각모음 / byrdispatch / Patch Notes. */
export const TOYBOX_WIDE_MAX_CLASS = "mx-auto w-full max-w-6xl";

/** Tailwind `max-w-2xl` = 42rem. Combo / Chemical X (and Transfigure detail). */
export const TOYBOX_NARROW_MAX_CLASS = "mx-auto w-full max-w-2xl";

export const TOYBOX_WIDE_SHELL_CLASS = `${TOYBOX_WIDE_MAX_CLASS} px-4 py-6`;
export const TOYBOX_NARROW_SHELL_CLASS = `${TOYBOX_NARROW_MAX_CLASS} px-4 py-6`;

/** Same wide max as History Course, tighter horizontal padding for the 조각모음 board. */
export const TOYBOX_WIDE_BOARD_SHELL_CLASS = `${TOYBOX_WIDE_MAX_CLASS} px-2 py-6 sm:px-3`;
