interface:
  display_name: "Supabase Migrations"
  short_description: "Apply repo SQL migrations via Supabase CLI"
  default_prompt: "Use $supabase-migrations to apply pending scare-the-spire Supabase migrations safely."
